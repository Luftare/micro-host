{
  "name": "micro-host",
  "version": "1.0.0",
  "description": "Host html files with dynamic routing based on folder structure",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LF",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.1"
  }
}
